import React, { useEffect, useState } from "react";
import Table from "./Table";

export default function Detail() {

    return (
        <div>
            'Hello, I am the detailed view of an intersection table. I should be hidden when the page changes or when there is no intersection table'
        </div>
    )
}